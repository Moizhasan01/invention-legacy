import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight } from "lucide-react";
import { PageHero } from "@/components/site/Editorial";
import { inventors } from "@/lib/inventors";
import { pageHead } from "@/lib/seo";
export const Route=createFileRoute("/timeline")({head:()=>pageHead("A Chronology of Black Invention — James E. Craver","Move through the 1753–2026 chronology assembled in Beyond the Echoes of Black History.","/timeline"),component:TimelinePage});
function TimelinePage(){return <><PageHero eyebrow="1753—2026" title="A chronology of Black invention"><p>Follow the manuscript’s profile order across invention, science, engineering, and technical contribution.</p></PageHero><section className="px-5 py-20 lg:px-10 lg:py-28"><div className="mx-auto max-w-[1000px]"><div className="relative border-l border-bronze/40 pl-7 sm:pl-12">{inventors.map((i,index)=><article key={i.profile} className="group relative grid gap-3 border-b border-border py-8 sm:grid-cols-[130px_1fr_auto] sm:items-center"><span className="absolute -left-[2.1rem] top-10 size-3 rounded-full border-2 border-background bg-gold sm:-left-[3.4rem]"/><div><p className="font-display text-3xl text-bronze">{i.year}</p><p className="mt-1 text-[10px] uppercase tracking-[.16em] text-muted-foreground">Profile {String(i.profile).padStart(2,'0')}</p></div><div><h2 className="font-display text-2xl">{i.name}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{i.contribution??'Documented profile in the manuscript chronology.'}</p></div><Link to="/inventors/$slug" params={{slug:i.slug}} className="mt-3 inline-flex size-10 items-center justify-center border border-border transition-colors hover:bg-primary hover:text-primary-foreground sm:mt-0" aria-label={`Open ${i.name} profile`}><ArrowUpRight className="size-4"/></Link></article>)}</div></div></section></>}
